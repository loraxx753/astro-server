# Force the Dockerfile so Railpack's Python 3.13 is not used to compile swisseph.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node --import tsx src/index.ts"
restartPolicyType = "ON_FAILURE"
